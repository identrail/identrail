# Core service settings
IDENTRAIL_SERVICE_NAME=identrail
IDENTRAIL_LOG_LEVEL=info
IDENTRAIL_PROVIDER=aws
IDENTRAIL_AWS_SOURCE=fixture
IDENTRAIL_AWS_REGION=us-east-1
# IDENTRAIL_AWS_PROFILE=default

# Auth (required)
IDENTRAIL_API_KEYS=replace-with-strong-read-key,replace-with-strong-write-key
IDENTRAIL_WRITE_API_KEYS=replace-with-strong-write-key
# For local Compose quickstart, use URL-safe password characters (hex works well).
# `make quickstart` auto-generates this value.
IDENTRAIL_POSTGRES_PASSWORD=replace-with-strong-postgres-password

# Optional scoped auth alternative (takes precedence over API_KEYS)
# IDENTRAIL_API_KEY_SCOPES=reader-key:read;writer-key:read,write
# Optional default scope fallback when token claims/headers are absent
# IDENTRAIL_DEFAULT_TENANT_ID=default
# IDENTRAIL_DEFAULT_WORKSPACE_ID=default
# Optional OIDC claim mapping overrides
# IDENTRAIL_OIDC_ISSUER_URL=https://keycloak.example.com/realms/identrail
# IDENTRAIL_OIDC_AUDIENCE=identrail-api
# IDENTRAIL_OIDC_WRITE_SCOPES=identrail.write,identrail.admin,write,admin
# IDENTRAIL_OIDC_TENANT_CLAIM=tenant_id
# IDENTRAIL_OIDC_WORKSPACE_CLAIM=workspace_id
# IDENTRAIL_OIDC_GROUPS_CLAIM=groups
# IDENTRAIL_OIDC_ROLES_CLAIM=roles
# Optional postgres scope isolation enforcement
# IDENTRAIL_POSTGRES_RLS_ENFORCED=false

# Locking backend (auto -> postgres in DB mode, in-memory otherwise)
IDENTRAIL_LOCK_BACKEND=auto
IDENTRAIL_LOCK_NAMESPACE=identrail

# Provider fixture mode defaults (kept for deterministic local scans)
IDENTRAIL_K8S_SOURCE=fixture
IDENTRAIL_AWS_FIXTURES=/app/testdata/aws/role_with_policies.json,/app/testdata/aws/role_with_urlencoded_trust.json
IDENTRAIL_K8S_FIXTURES=/app/testdata/kubernetes/service_account_payments.json,/app/testdata/kubernetes/cluster_role_cluster_admin.json,/app/testdata/kubernetes/role_binding_cluster_admin.json,/app/testdata/kubernetes/pod_payments.json
# Optional live AWS collection via SDK:
# IDENTRAIL_AWS_SOURCE=sdk
# IDENTRAIL_AWS_REGION=us-east-1
# IDENTRAIL_AWS_PROFILE=default
# Optional live k8s collection via kubectl
# IDENTRAIL_K8S_SOURCE=kubectl
# IDENTRAIL_KUBECTL_PATH=/usr/bin/kubectl
# IDENTRAIL_KUBE_CONTEXT=dev-cluster

# Worker schedule
IDENTRAIL_SCAN_INTERVAL=15m
IDENTRAIL_WORKER_RUN_NOW=true
# Optional scheduled repository scans (off by default)
IDENTRAIL_WORKER_REPO_SCAN_ENABLED=false
IDENTRAIL_WORKER_REPO_SCAN_RUN_NOW=false
IDENTRAIL_WORKER_REPO_SCAN_INTERVAL=1h
# Comma-separated targets, required when worker repo scan is enabled
# IDENTRAIL_WORKER_REPO_SCAN_TARGETS=owner/repo,trusted-org/security-repo
# Optional per-worker overrides (0 uses service defaults)
# IDENTRAIL_WORKER_REPO_SCAN_HISTORY_LIMIT=500
# IDENTRAIL_WORKER_REPO_SCAN_MAX_FINDINGS=200

# Repo exposure scanner defaults
IDENTRAIL_SCAN_QUEUE_MAX_PENDING=25
IDENTRAIL_REPO_SCAN_QUEUE_MAX_PENDING=100
IDENTRAIL_REPO_SCAN_ENABLED=false
IDENTRAIL_REPO_SCAN_HISTORY_LIMIT=500
IDENTRAIL_REPO_SCAN_MAX_FINDINGS=200
IDENTRAIL_REPO_SCAN_HISTORY_LIMIT_MAX=5000
IDENTRAIL_REPO_SCAN_MAX_FINDINGS_MAX=1000
# Required when IDENTRAIL_REPO_SCAN_ENABLED=true (comma-separated, supports prefix wildcard with *)
# IDENTRAIL_REPO_SCAN_ALLOWLIST=trusted-org/*,owner/repo

# API controls
IDENTRAIL_RATE_LIMIT_RPM=120
IDENTRAIL_RATE_LIMIT_BURST=20
# Local dashboard origin for out-of-box web -> API calls in Docker Compose quickstart.
IDENTRAIL_CORS_ALLOWED_ORIGINS=http://localhost:8081
# Comma-separated trusted proxy IPs/CIDRs (empty = trust none, safest default)
# IDENTRAIL_TRUSTED_PROXIES=10.0.0.0/8,127.0.0.1

# Optional alerts
# IDENTRAIL_ALERT_WEBHOOK_URL=https://alerts.example.com/identrail
# IDENTRAIL_ALERT_MIN_SEVERITY=high
# IDENTRAIL_ALERT_HMAC_SECRET=replace-with-secret
# IDENTRAIL_ALERT_MAX_RETRIES=2
# IDENTRAIL_ALERT_RETRY_BACKOFF=1s

# Web build API URL (must be HTTPS in production unless localhost)
VITE_IDENTRAIL_API_URL=http://localhost:8080
